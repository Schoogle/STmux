# SimpleTmux (STmux)
Simple Tmux session overview and manager tool\
For installation with uv, run `uv tool install git+https://github.com/Schoogle/STmux.git`
This will install the program, which will be available with `stmux` in the terminal